#define _CRT_SECURE_NO_WARNINGS
#include <stdio.h>
#include <stdlib.h>

typedef struct NODE {
	char* str;
	NODE* next;
}NODE;

void reverse(char* str)
{
	char* p, * q;
	
}

int main()
{
	NODE* front = NULL, * rear = NULL;
	char str[16];

	
		return 0;
}